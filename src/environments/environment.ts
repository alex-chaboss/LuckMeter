// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The history of file replacements can be found in `angular.json`.

export const environment = {
  clientId: 'AfTSLsuk38qdUS_7vDya9giMdLzs393vBmbXCcltvQulTm0Hp5dXT4YB_8dijo4sVzo-ZeiBLA9jIy2m',
  production: false,
  CENTER_INTERVAL_CHANGE: 600000,
  plans: [
    'P-1M807412F8342413TMA6OVII',
    'P-1M807412F8342413TMA6OVII',
    'P-1M807412F8342413TMA6OVII',
    'P-1M807412F8342413TMA6OVII',
    'P-1M807412F8342413TMA6OVII',
  ]
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
